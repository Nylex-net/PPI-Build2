const assert = require("assert");
const MSConnect = require("./Validate2.js");

describe("Connect to a Microsoft Access database", () => {
    it("Our response from attempting to connect", () => {
      assert.getEmpl();
    });
  });